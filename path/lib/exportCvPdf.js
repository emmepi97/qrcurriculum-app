export async function exportCvPdf(sheetNode,filename='CV.pdf'){if(!sheetNode)return;const html2pdf=(await import('html2pdf.js')).default;await new Promise(r=>setTimeout(r,120));await html2pdf().set({margin:0,filename,pagebreak:{mode:['avoid-all']},image:{type:'jpeg',quality:.98},html2canvas:{scale:3,useCORS:true,backgroundColor:'#ffffff',windowWidth:sheetNode.scrollWidth,windowHeight:sheetNode.scrollHeight},jsPDF:{unit:'mm',format:'a4',orientation:'portrait',compress:true}}).from(sheetNode).save()}
export async function exportElementPdf(node,filename='report.pdf'){if(!node)return;const html2pdf=(await import('html2pdf.js')).default;await html2pdf().set({margin:8,filename,image:{type:'jpeg',quality:.98},html2canvas:{scale:2,useCORS:true,backgroundColor:'#ffffff'},jsPDF:{unit:'mm',format:'a4',orientation:'portrait',compress:true}}).from(node).save()}
export default exportCvPdf;
