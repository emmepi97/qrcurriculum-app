'use client';
import { useEffect,useState } from 'react';
import supabase from '@/lib/supabaseClient';
import AppTopbar from '@/components/AppTopbar';
export default function ConsultationPage(){const[session,setSession]=useState(null);const[form,setForm]=useState({name:'',email:'',phone:'',message:''});const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);useEffect(()=>{supabase.auth.getSession().then(({data})=>{setSession(data.session);if(data.session?.user?.email)setForm(f=>({...f,email:data.session.user.email}))})},[]);async function submit(e){e.preventDefault();setBusy(true);setMsg('');try{const payload={user_id:session?.user?.id||null,email:form.email,name:form.name,phone:form.phone,message:form.message,source:'consultation_form_submit',status:'requested',price_eur:25};const res=await fetch('/api/consultation-click',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(!res.ok)throw new Error('Errore invio richiesta');setMsg('Richiesta inviata. Ti contatteremo a breve.')}catch(e){setMsg(e.message)}finally{setBusy(false)}}return <><AppTopbar email={session?.user?.email}/><main className="consultPageV21"><section className="consultHeroV21"><div><span className="consultBadge">CV Consulting</span><h1>Trasforma il CV in una pagina professionale che porta colloqui</h1><p>Analisi rapida, consigli pratici e ottimizzazione di contenuti, keyword, portfolio, video e Job Match Score.</p><div className="consultHeroActions"><a className="btn light" href="#request">Richiedi consulenza</a><span>Servizio CV da 25€</span></div></div><aside><strong>Output</strong><p>Criticità chiare, priorità di miglioramento e suggerimenti concreti per rendere il profilo più competitivo.</p></aside></section><section className="consultValueGrid"><article><span>01</span><h2>Analisi contenuti</h2><p>Verifichiamo se ruolo, esperienze, competenze e risultati sono leggibili in pochi secondi.</p></article><article><span>02</span><h2>Job Match</h2><p>Controlliamo keyword mancanti, coerenza con annunci e punti da rafforzare.</p></article><article><span>03</span><h2>Portfolio</h2><p>Valutiamo anche video, descrizione personale, progetti, radar skill e recensioni.</p></article></section><section className="consultProcess"><div><span className="consultBadge">Metodo</span><h2>Come funziona</h2><p>Un flusso semplice pensato per persone che vogliono migliorare il profilo senza perdersi in teoria.</p></div><ol><li><strong>Invii richiesta</strong><span>Indichi obiettivo, ruolo o link del portfolio.</span></li><li><strong>Analisi pratica</strong><span>Vengono evidenziati punti deboli e priorità.</span></li><li><strong>Ottimizzazione</strong><span>Ricevi suggerimenti su contenuti, struttura e keyword.</span></li></ol></section><section id="request" className="consultRequestPanel"><div><span className="consultBadge">Richiesta</span><h2>Richiedi la consulenza</h2><p>Incolla il link del CV/portfolio o descrivi l’obiettivo professionale. La richiesta viene salvata nel pannello admin.</p></div><form onSubmit={submit} className="consultForm"><label>Nome<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></label><label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label><label>Telefono<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label className="wide">Obiettivo / link portfolio<textarea rows={6} value={form.message} onChange={e=>setForm({...form,message:e.target.value})} placeholder="Es. Voglio candidarmi come Production Planner. Link portfolio: ..."/></label><button className="btn primary" disabled={busy}>{busy?'Invio...':'Invia richiesta'}</button>{msg&&<p className="success">{msg}</p>}</form></section></main></>}
