'use client';
import { useEffect,useState } from 'react';
import { useRouter } from 'next/navigation';
import supabase from '@/lib/supabaseClient';
import { useLang } from '@/components/LanguageProvider';
import AppTopbar from '@/components/AppTopbar';
export default function ConsultationPage(){const router=useRouter();const{lang}=useLang();const[session,setSession]=useState(null);const[form,setForm]=useState({name:'',email:'',phone:'',message:''});const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);useEffect(()=>{supabase.auth.getSession().then(({data})=>{setSession(data.session);if(data.session?.user?.email)setForm(f=>({...f,email:data.session.user.email}))})},[]);async function submit(e){e.preventDefault();setBusy(true);setMsg('');try{const payload={user_id:session?.user?.id||null,email:form.email,name:form.name,phone:form.phone,message:form.message,source:'consultation_form_submit',status:'requested',price_eur:25};const res=await fetch('/api/consultation-click',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(!res.ok)throw new Error('Errore invio richiesta');setMsg('Richiesta inviata. Ti contatteremo a breve.')}catch(e){setMsg(e.message)}finally{setBusy(false)}}return <><AppTopbar email={session?.user?.email}/><section className='heroPanel'><div className='cleanHero'><div className='eyebrow'>CV CONSULTING</div><h1>Trasforma il tuo CV in uno strumento che genera colloqui</h1><p>Analizziamo il tuo CV, individuiamo i punti deboli e lo ottimizziamo per recruiter, ATS e Job Match Score.</p></div><div className='cvHeroCard'><span>Offerta</span><strong>Analisi gratuita + Ottimizzazione CV a 25€</strong><p>Ricevi suggerimenti pratici e una versione migliorata del CV pronta per le candidature.</p></div></section><main className='pageWrap consultationLanding'><section className='directoryValueGrid'><article className='smartSection'><h2>✅ Analisi completa</h2><p>Valutiamo struttura, leggibilità, competenze e punti deboli.</p></article><article className='smartSection'><h2>✅ ATS Friendly</h2><p>Ottimizzazione per software di screening e recruiter.</p></article><article className='smartSection'><h2>✅ Job Match</h2><p>Miglioramento delle keyword per aumentare la compatibilità.</p></article></section><section className='consultationGrid'><article className='consultationCard'><h2>Cosa ottieni</h2><ul><li>Analisi del CV attuale.</li><li>Elenco criticità.</li><li>Consigli personalizzati.</li><li>Revisione contenuti e layout.</li><li>Miglioramento Job Match Score.</li><li>Supporto candidatura.</li></ul><h2>Per chi è utile</h2><ul><li>Studenti</li><li>Professionisti</li><li>Manager</li><li>Chi cerca lavoro</li></ul></article><article className='consultationCard'><h2>Richiedi la consulenza</h2><form onSubmit={submit} className='quickForm'><label>Nome<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></label><label>Email<input type='email' value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label><label>Telefono<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Messaggio<textarea rows={5} value={form.message} onChange={e=>setForm({...form,message:e.target.value})} placeholder='Incolla il link del CV o descrivi il tuo obiettivo professionale.'/></label><button className='btn primary big' disabled={busy}>{busy?'Invio...':'Richiedi consulenza'}</button>{msg&&<p>{msg}</p>}</form></article></section></main></>}