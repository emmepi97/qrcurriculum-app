'use client';import { useEffect,useState } from 'react';import { useRouter } from 'next/navigation';import supabase from '@/lib/supabaseClient';import { useLang } from '@/components/LanguageProvider';import AppTopbar from '@/components/AppTopbar';import MonetizationPanel from '@/components/MonetizationPanel';export default function MonetizationPage(){const router=useRouter();const{t,lang}=useLang();const[session,setSession]=useState(null);const[loading,setLoading]=useState(true);useEffect(()=>{supabase.auth.getSession().then(({data})=>{if(!data.session){router.push('/login?next=/monetization');return}setSession(data.session);setLoading(false)})},[router]);if(loading)return <p className="pageWrap">{t.loading}</p>;return <><AppTopbar email={session?.user?.email}/><section className="heroPanel"><div className="cleanHero"><div className="eyebrow">{t.monetization}</div><h1>{lang==='en'?'Plans, reviews and professional availability':'Piani, recensioni e disponibilità professionale'}</h1><p>{lang==='en'?'Visible to logged-in users only.':'Visibile solo agli utenti loggati.'}</p></div></section><MonetizationPanel userId={session.user.id}/></>}
