'use client';import { useEffect,useState } from 'react';import { useParams,useRouter } from 'next/navigation';import Link from 'next/link';import supabase from '@/lib/supabaseClient';import { useLang } from '@/components/LanguageProvider';export default function ReviewRequestPage(){const{token}=useParams();const router=useRouter();const{lang}=useLang();const[session,setSession]=useState(null);const[request,setRequest]=useState(null);const[loading,setLoading]=useState(true);const[msg,setMsg]=useState('');const[form,setForm]=useState({reviewer_name:'',reviewer_role:'',reviewer_company:'',relationship:'',rating:5,review_text:'',consent_publication:true});useEffect(()=>{supabase.auth.getSession().then(({data})=>{setSession(data.session);if(!data.session){setLoading(false);return}load()})},[token]);async function load(){const{data,error}=await supabase.from('review_requests').select('*').eq('token',token).eq('status','open').maybeSingle();if(error||!data){setMsg(lang==='en'?'Request not found or closed.':'Richiesta non trovata o chiusa.');setLoading(false);return}setRequest(data);setForm(f=>({...f,reviewer_name:data.reviewer_name||'',reviewer_role:data.reviewer_role||'',reviewer_company:data.reviewer_company||''}));setLoading(false)}async function submit(e){e.preventDefault();setMsg('');if(!session?.user?.id){router.push(`/login?next=/reviews/${token}`);return}if(!form.review_text.trim()){setMsg('Scrivi il testo della recensione.');return}const payload={...form,owner_user_id:request.owner_user_id,reviewer_user_id:session.user.id,request_id:request.id,rating:Number(form.rating),status:'pending'};const{error}=await supabase.from('portfolio_reviews').insert(payload);if(error){setMsg(error.message);return}await supabase.from('review_requests').update({status:'completed'}).eq('id',request.id);setMsg(lang==='en'?'Review sent.':'Recensione inviata.')}if(loading)return <p className="pageWrap">Loading...</p>;if(!session)return <section className="pageWrap smartSection"><h1>Login richiesto</h1><p>Per lasciare recensione devi accedere.</p><Link className="btn primary" href={`/login?next=/reviews/${token}`}>Login</Link></section>;if(!request)return <p className="pageWrap error">{msg}</p>;return <section className="pageWrap smartSection"><Link href="/">← Home</Link><h1>{lang==='en'?'Write a reference':'Scrivi una referenza'}</h1>{request.message&&<p className="hint">{request.message}</p>}<form className="quickForm" onSubmit={submit}><label>Nome<input value={form.reviewer_name} onChange={e=>setForm({...form,reviewer_name:e.target.value})} required/></label><label>Ruolo<input value={form.reviewer_role} onChange={e=>setForm({...form,reviewer_role:e.target.value})}/></label><label>Azienda<input value={form.reviewer_company} onChange={e=>setForm({...form,reviewer_company:e.target.value})}/></label><label>Relazione<input value={form.relationship} onChange={e=>setForm({...form,relationship:e.target.value})}/></label><label>Valutazione<select value={form.rating} onChange={e=>setForm({...form,rating:e.target.value})}>{[5,4,3,2,1].map(x=><option key={x} value={x}>{x}/5</option>)}</select></label><label>Recensione<textarea value={form.review_text} onChange={e=>setForm({...form,review_text:e.target.value})} required/></label><label className="check"><input type="checkbox" checked={form.consent_publication} onChange={e=>setForm({...form,consent_publication:e.target.checked})}/> Autorizzo pubblicazione</label><button className="btn primary">Invia recensione</button>{msg&&<p>{msg}</p>}</form></section>}