'use client';
import {useEffect,useRef,useState} from 'react';import {useParams,useSearchParams} from 'next/navigation';import QRCode from 'qrcode';import {supabase} from '@/lib/supabaseClient';import {makeQrUrl} from '@/lib/helpers';import CvTemplate from '@/components/CvTemplate';
export default function PublicQrCvPage(){const{slug}=useParams();const params=useSearchParams();const[loading,setLoading]=useState(true);const[error,setError]=useState('');const[data,setData]=useState(null);const[qrCode,setQrCode]=useState('');const tracked=useRef(false);useEffect(()=>{async function load(){const{data:p,error:e}=await supabase.from('personal_info').select('*').eq('public_slug',slug).eq('is_public',true).maybeSingle();if(e||!p){setError('Profilo non trovato o non pubblico.');setLoading(false);return}const next={personal_info:p};for(const t of ['work_experiences','educations','languages','skills','awards','projects','case_studies']){const{data:rows}=await supabase.from(t).select('*').eq('user_id',p.user_id).eq('is_hidden',false).order('created_at',{ascending:false});next[t]=rows||[]}setData(next);QRCode.toDataURL(makeQrUrl(slug),{width:180,margin:1}).then(setQrCode);setLoading(false);if(!tracked.current){tracked.current=true;const base={profile_user_id:p.user_id,public_slug:slug,path:window.location.pathname,referrer:document.referrer||'',user_agent:navigator.userAgent||''};await supabase.from('analytics_events').insert({...base,event_type:'profile_view'});if(params.get('qr')==='1')await supabase.from('analytics_events').insert({...base,event_type:'qr_scan'});}}if(slug)load()},[slug,params]);async function downloadPdf(){const html2pdf=(await import('html2pdf.js')).default;html2pdf().set({margin:6,filename:'CV.pdf',image:{type:'jpeg',quality:.98},html2canvas:{scale:2,useCORS:true},jsPDF:{unit:'mm',format:'a4',orientation:'portrait'}}).from(document.getElementById('cv-document')).save()}if(loading)return <main className="publicPage"><p>Caricamento profilo...</p></main>;if(error)return <main className="publicPage"><p className="error">{error}</p></main>;return <main className="publicPage"><div className="publicActions"><button className="btn primary" onClick={downloadPdf}>Scarica PDF</button></div><CvTemplate data={data} qrCode={qrCode}/></main>}
