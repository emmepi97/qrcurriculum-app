'use client';
import {useEffect,useState} from 'react';
const steps=[['Benvenuto','Qui gestisci il CV online.'],['Link corretto','Il link pubblico usa il dominio reale del sito.'],['Più elementi','Ogni sezione può contenere più righe.']];
export default function FirstAccessTour(){const[vis,setVis]=useState(false);const[i,setI]=useState(0);useEffect(()=>{if(!localStorage.getItem('cv-tour-done-v4'))setVis(true)},[]);function close(){localStorage.setItem('cv-tour-done-v4','1');setVis(false)}if(!vis)return null;return <div className="tourOverlay"><div className="tourCard"><div className="tourProgress"><span style={{width:`${((i+1)/steps.length)*100}%`}}/></div><p className="tourStep">Passo {i+1} di {steps.length}</p><h2>{steps[i][0]}</h2><p>{steps[i][1]}</p><div className="tourActions"><button className="btn" onClick={close}>Salta</button><button className="btn primary" onClick={()=>i===steps.length-1?close():setI(i+1)}>{i===steps.length-1?'Inizia':'Avanti'}</button></div></div></div>}
